{
  "name": "quota-gate",
  "version": "0.1.0",
  "description": "Provider-style rate limiting for model-serving APIs (TypeScript port).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
